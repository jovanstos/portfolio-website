export interface project {
    id: number;
    title: string;
    description: string;
    url: string;
    imagedescription: string;
    imageurl: string;
}

export interface projectContent {
    id: number;
    text: string;
    imagedescription: string;
    imageurl: string;
}